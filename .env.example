PORT=3000
HOST=0.0.0.0
BASE_URL=https://admin.amarpet.com/api/v1
MCP_PATH=/mcp
# Supports: single (API_KEY=abc), comma-separated (API_KEY=abc,def), or JSON array (API_KEY='["abc","def"]')
API_KEY=alamin899
# When HOST=0.0.0.0, include every hostname/IP clients use in the MCP URL (Host header), comma-separated:
ALLOWED_HOSTS=127.0.0.1,165.232.154.13,mcp.yourdomain.com